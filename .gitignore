# Build outputs
/build/
/native/build/
.gradle/

# IDE / local machine config (never commit)
/.idea/
*.iml
local.properties

# OS / editor
*.log
.DS_Store

# Kotlin build session cache
/.kotlin/

# Local Claude Code guidance (not for the public repo)
/CLAUDE.md
